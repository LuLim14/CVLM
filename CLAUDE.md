This is the project of compression text using visual encoders. The core idea is to use visual embedder for text embeddings from text encoder to compress the text tokens and save the accuracy for text reconstruction.


